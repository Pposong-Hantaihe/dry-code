import { IClone, ICloneValidator, IMapFrame, IOptions, IStore } from './interfaces';
import { ITokensMap } from '.';
import EventEmitter = require('eventemitter3');
export declare class RabinKarp {
    private readonly options;
    private readonly eventEmitter;
    private readonly cloneValidators;
    constructor(options: IOptions, eventEmitter: EventEmitter, cloneValidators: ICloneValidator[]);
    run(tokenMap: ITokensMap, store: IStore<IMapFrame>): Promise<IClone[]>;
    private validate;
    private static createClone;
    private static enlargeClone;
}
