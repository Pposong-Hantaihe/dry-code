import { IMapFrame, IToken, ITokensMap } from '@jscpd/core';
export declare class TokensMap implements ITokensMap, Iterator<IMapFrame | boolean>, Iterable<IMapFrame | boolean> {
    private readonly id;
    private readonly data;
    private readonly tokens;
    private readonly format;
    private readonly options;
    private position;
    private hashMap;
    constructor(id: string, data: string, tokens: IToken[], format: string, options: any);
    getTokensCount(): number;
    getId(): string;
    getLinesCount(): number;
    getFormat(): string;
    [Symbol.iterator](): Iterator<IMapFrame | boolean>;
    next(): IteratorResult<IMapFrame | boolean>;
}
export declare function generateMapsForFormats(id: string, data: string, tokens: IToken[], options: any): TokensMap[];
export declare function createTokensMaps(id: string, data: string, tokens: IToken[], options: any): TokensMap[];
