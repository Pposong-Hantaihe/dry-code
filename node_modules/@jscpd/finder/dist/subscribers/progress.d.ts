import { DetectorEvents, IHandler, IOptions, ISubscriber } from '@jscpd/core';
export declare class ProgressSubscriber implements ISubscriber {
    private readonly options;
    constructor(options: IOptions);
    subscribe(): Partial<Record<DetectorEvents, IHandler>>;
}
