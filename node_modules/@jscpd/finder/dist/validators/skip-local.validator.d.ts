import { IClone, ICloneValidator, IOptions, IValidationResult } from '@jscpd/core';
export declare class SkipLocalValidator implements ICloneValidator {
    validate(clone: IClone, options: IOptions): IValidationResult;
    shouldSkipClone(clone: IClone, options: IOptions): boolean;
    private static isRelative;
}
