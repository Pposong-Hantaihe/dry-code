import { IClone, IOptions } from '@jscpd/core';
import { IReporter } from '..';
export declare class XcodeReporter implements IReporter {
    private readonly options;
    constructor(options: IOptions);
    report(clones: IClone[]): void;
    private cloneFound;
}
