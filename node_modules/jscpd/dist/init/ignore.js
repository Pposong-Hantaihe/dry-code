"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const fs_1 = require("fs");
const gitignoreToGlob = require('gitignore-to-glob');
function initIgnore(options) {
    const ignore = options.ignore || [];
    if (options.gitignore && fs_1.existsSync(process.cwd() + '/.gitignore')) {
        let gitignorePatterns = gitignoreToGlob(process.cwd() + '/.gitignore') || [];
        gitignorePatterns = gitignorePatterns.map((pattern) => pattern.substr(pattern.length - 1) === '/' ? `${pattern}**/*` : pattern);
        ignore.push(...gitignorePatterns);
        ignore.map((pattern) => pattern.replace('!', ''));
    }
    return ignore;
}
exports.initIgnore = initIgnore;
//# sourceMappingURL=ignore.js.map